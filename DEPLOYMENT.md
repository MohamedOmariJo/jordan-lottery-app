# 🚀 دليل النشر السريع على Streamlit Cloud

## خطوات النشر

### 1. رفع الملفات إلى GitHub
تأكد من وجود الملفات التالية في المستودع:
```
jordan-lottery-app/
├── app.py
├── requirements.txt
├── history.xlsx
├── README.md
└── .streamlit/
    └── config.toml
```

### 2. نشر التطبيق على Streamlit Cloud

1. اذهب إلى [share.streamlit.io](https://share.streamlit.io)
2. سجّل الدخول بحساب GitHub
3. اضغط على "New app"
4. املأ البيانات:
   - **Repository:** MohamedOmariJo/jordan-lottery-app
   - **Branch:** main
   - **Main file path:** app.py
5. اضغط "Deploy!"

### 3. الحصول على رابط التطبيق
بعد النشر بنجاح، ستحصل على رابط مثل:
```
https://your-app-name.streamlit.app
```

### 4. مشاركة الرابط
- شارك الرابط مع أصدقائك
- التطبيق سيعمل تلقائياً بدون الحاجة لتحميل البيانات
- البيانات تُحدّث تلقائياً من `history.xlsx`

## 📊 تحديث البيانات

### طريقة التحديث (مرتين في الأسبوع):

#### عبر الويب (الطريقة الأسهل):
1. اذهب إلى المستودع على GitHub
2. افتح ملف `history.xlsx`
3. اضغط على أيقونة القلم (Edit)
4. أضف السحب الجديد
5. اضغط "Commit changes"

#### عبر Git:
```bash
# 1. افتح الملف وأضف السحب الجديد
# 2. احفظ التغييرات

# 3. ارفع التحديثات
git add history.xlsx
git commit -m "تحديث البيانات - سحب رقم XXX - تاريخ DD/MM/YYYY"
git push origin main
```

### إعادة تشغيل التطبيق:
بعد تحديث `history.xlsx`:
1. افتح التطبيق
2. اضغط على "🔄 إعادة تحميل البيانات" في الشريط الجانبي
3. ستظهر البيانات المحدثة فوراً

## ⚡ نصائح مهمة

### للحصول على أفضل أداء:
- حافظ على حجم ملف `history.xlsx` معقول (أقل من 5 ميغابايت)
- لا تضف أعمدة إضافية غير ضرورية
- استخدم تنسيق Excel القياسي (.xlsx)

### الأمان:
- المستودع يجب أن يكون عاماً (Public)
- لا تضع معلومات حساسة في الملفات

### الصيانة:
- تحقق من عمل التطبيق بعد كل تحديث
- احتفظ بنسخة احتياطية من `history.xlsx`

## 🔧 استكشاف الأخطاء

### المشكلة: التطبيق لا يحمّل البيانات
**الحل:**
- تأكد من أن المستودع عام (Public)
- تحقق من اسم الملف: `history.xlsx` (بدون فراغات)
- تأكد من صحة الرابط في `app.py`:
  ```python
  DEFAULT_GITHUB_URL = "https://raw.githubusercontent.com/MohamedOmariJo/jordan-lottery-app/main/history.xlsx"
  ```

### المشكلة: البيانات قديمة
**الحل:**
- اضغط "🔄 إعادة تحميل البيانات"
- إذا لم تتحدث، امسح الكاش:
  - Settings → Clear cache
  - أعد تشغيل التطبيق

### المشكلة: خطأ في قراءة الملف
**الحل:**
- تحقق من وجود الأعمدة: N1, N2, N3, N4, N5, N6
- تأكد من أن الأرقام بين 1 و 32
- تحقق من عدم وجود صفوف فارغة

## 📱 للمستخدمين (أصدقائك)

عند إرسال الرابط لأصدقائك، أخبرهم:
1. افتح الرابط
2. التطبيق سيعمل مباشرة
3. لا حاجة لتحميل أي ملفات
4. يمكنهم استخدام جميع الميزات فوراً

---

**في حالة وجود مشاكل:**
افتح Issue في المستودع على GitHub
