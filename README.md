# 🎰 نظام توليد وفحص تذاكر اليانصيب الأردني

نظام ذكي لتوليد وفحص تذاكر اليانصيب الأردني باستخدام التحليل الإحصائي والتاريخي للسحوبات السابقة.

## ✨ الميزات

- 🎲 **توليد تذاكر ذكية** بناءً على معايير محددة
- 🕵️ **فحص تذكرة تاريخياً** للتحقق من التطابقات السابقة
- 📊 **تحليل إحصائي** شامل للأرقام والمتتاليات
- 🔄 **تحميل تلقائي للبيانات** من GitHub
- 📈 **استراتيجيات متعددة** (ساخنة، باردة، متوازنة)

## 🚀 كيفية الإعداد

### 1. إعداد المستودع على GitHub

1. قم بإنشاء مستودع جديد على GitHub (مثلاً: `jordan-lottery-app`)
2. اجعل المستودع **عاماً (Public)**
3. ارفع ملف `history.xlsx` إلى المستودع

### 2. الحصول على رابط الملف

1. افتح ملف `history.xlsx` في GitHub
2. اضغط على زر **"Raw"**
3. انسخ الرابط من شريط العنوان

**مثال على الرابط:**
```
https://raw.githubusercontent.com/MohamedOmariJo/jordan-lottery-app/main/history.xlsx
```

### 3. تحديث الكود

افتح ملف `app.py` وابحث عن السطر التالي (السطر 31 تقريباً):

```python
DEFAULT_GITHUB_URL = "https://raw.githubusercontent.com/MohamedOmariJo/jordan-lottery-app/main/history.xlsx"
```

واستبدله برابط ملفك الخاص:

```python
DEFAULT_GITHUB_URL = "رابط_ملفك_هنا"
```

### 4. نشر التطبيق

#### على Streamlit Cloud:

1. سجل الدخول إلى [Streamlit Cloud](https://streamlit.io/cloud)
2. اضغط على "New app"
3. اختر المستودع والملف `app.py`
4. اضغط على "Deploy"

#### تشغيل محلي:

```bash
pip install -r requirements.txt
streamlit run app.py
```

## 📝 متطلبات ملف البيانات

يجب أن يحتوي ملف Excel على الأعمدة التالية:

- `N1`, `N2`, `N3`, `N4`, `N5`, `N6` - أرقام السحب (يجب أن تكون بين 1 و 32)
- `رقم السحب` أو `DrawID` (اختياري) - رقم تعريف السحب

## 🔄 تحديث البيانات

بعد كل سحب جديد (الأحد والأربعاء):

1. افتح ملف `history.xlsx` على جهازك المحلي
2. أضف بيانات السحب الجديد
3. ارفع الملف المحدث إلى GitHub (استبدل الملف القديم)
4. التطبيق سيحمل البيانات الجديدة تلقائياً (قد يستغرق بضع دقائق بسبب التخزين المؤقت)

### لإجبار التحديث الفوري:

اضغط على "🔄 إعادة تحميل من GitHub" في الشريط الجانبي للتطبيق

## 🛠️ التقنيات المستخدمة

- **Streamlit** - بناء واجهة المستخدم
- **Pandas** - معالجة البيانات
- **Requests** - تحميل البيانات من GitHub

## 👨‍💻 المطور

**محمد العمري**

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي.

---

**ملاحظة:** هذا التطبيق للأغراض الترفيهية والتعليمية فقط. لا يضمن الفوز في اليانصيب.
